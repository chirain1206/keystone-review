# 任务清单：WoW M+ AI 复盘教练

> 阶段 3（技术设计）产出 · 依据 docs/TECH-DESIGN.md
> 每个任务 ≈ 一次开发回合（一次 PR）粒度，带验收标准与依赖。QA 阶段将逐条对照 FR-x 验收。

## 分批计划

- **批次 1（地基）**：T1–T4
- **批次 2（核心闭环）**：T5–T7
- **批次 3（数据接入与防护）**：T8–T9
- **批次 4（外围与收尾）**：T10–T13
- **批次 5（RAG 社区知识库，用户拍板新增）**：T14–T18
- **批次 6（高阶技巧主动挖掘，用户新要求）**：T19–T20

| # | 任务 | 验收标准 | 依赖 |
|---|---|---|---|
| T1 | 项目脚手架：Next.js(App Router)+TS 仓库、Supabase 项目创建、Vercel 项目关联与首次部署、环境变量管理、/api/health | 仓库 build 通过；Vercel 部署成功；/api/health 返回 ok；本地与线上环境变量隔离可用 | — |
| T2 | 数据模型与 RLS：建表（profiles/reports/processed_logs/report_chapters/conversations/messages/shares）+ 迁移脚本 + RLS 策略 | 迁移可重复执行；RLS 生效：用户 A 无法读写用户 B 的任何行；删除 report 级联删除章节/问答/分享 | T1 |
| T3 | 账号体系（FR-7）：Supabase passwordless 邮箱验证码 + Resend SMTP、登录/登出、未登录引导、验证码频控（错 5 次锁 10 分钟） | FR-7 验收标准逐条通过；验证码邮件送达（Resend 日志可见） | T2 |
| T4 | 浏览器端日志解析器 + FR-10 预处理：Web Worker 分块读取 WoWCombatLog.txt，识别大秘境战斗分段，按关键事件白名单提取（技能/爆发/CD/打断/死亡/增益减益/易伤/移动），产出结构化 JSON | 用样例文件（≥3 个真实 log，含噪声）测试：正确列出战斗（副本/层数/时间/专精）；结构化数据 ≤50K token/场且相对原始缩减 ≥90%；时间戳与原始一致；非日志文件明确报错（FR-2、FR-10 验收标准） | T1 |
| T5 | 复盘生成管线（FR-4）：6 章并行流式生成接口（SSE）、每章独立存储与幂等、断点重试（单章接口）、每章输出封顶 1800 token、章节级数据切片 + DeepSeek 上下文缓存、进度回传 | FR-4 验收标准通过：报告 p95 ≤120s；章节失败可单独重试；每场复盘的 token 用量与成本可观测（日志字段） | T2, T4 |
| T6 | 战术意图识别专项（FR-5）：意图判断提示词（先查战术合理性再下结论）、报告第 5 章实现、样例集构造（≥10 个"看似失误实为正确决策"案例 + ≥5 个真实失误案例）与评测脚本 | 开发自测：构造样例 ≥2 个通过；样例集文件与评测脚本就绪（QA 阶段跑 ≥80% 通过率） | T5 |
| T7 | 对话问答（FR-6）：问答流式接口、上下文组装（结构化事件切片+历史轮次）、10 轮上限、违规问题拒绝（代练/RMT 等）、"通用建议非本场数据"标注 | FR-6 验收标准逐条通过 | T5 |
| T8 | WCL 链接接入（FR-1/FR-3）：WCL v2 API client、链接校验与元数据拉取（副本/层数/角色专精）、对比链接基准数据、失败降级（提示改用文件/不含对比章节） | FR-1 验收标准通过（含全球服/国服链接）；FR-3 对比部分通过；WCL 配额耗尽时优雅降级不阻塞复盘 | T2, T4 |
| T9 | 额度与防滥用（FR-7 额度部分）：每账号每日 3 次复盘计数（按用户时区自然日）、用尽提示、Turnstile 人机验证接入（登录/创建报告接口）、IP/邮箱频控 | 同账号同日第 4 次被拒且提示准确；换时区边界正确；绕过人机验证的脚本请求被拒 | T3, T5 |
| T10 | 历史记录（FR-8）：列表/详情接口与页面、删除（级联） | FR-8 验收标准逐条通过；用户 A 无法访问用户 B 数据（接口层测试） | T3, T5 |
| T11 | 一键分享（FR-9）：shares 表 + 128-bit 随机 token、公开只读分享页 GET /s/:token、开启/关闭 | FR-9 验收标准逐条通过；token 不可枚举（随机性测试）；分享页无任何写操作接口 | T5 |
| T12 | 前端界面整合：首页（粘贴链接/选文件）、战斗选择页、报告页（章节进度+渲染+问答框）、我的复盘页、登录页、分享页、隐私政策/用户协议/免责声明页；响应式适配 | 全部页面可用、首屏 ≤3s、移动端自适应；报告页章节进度与重试交互正确 | T3, T5, T7, T10, T11 |
| T13 | 安全与合规加固：HTTPS、接口鉴权与数据隔离复核、上传数据结构校验（≤50K token 预算服务端再校验）、隐私政策/免责声明文案（"非暴雪官方产品，与暴雪娱乐无关"）、错误提示全覆盖 | 非功能需求（安全/隐私/合规）检查表逐项通过；未登录访问受保护接口一律 401 | T2, T3, T11 |
| T14 | 知识库数据模型与检索（FR-11）：迁移 0003_kb_documents.sql（create extension vector + 表 kb_documents：chunk_text、embedding vector(1024)、meta jsonb(class/spec/dungeon/patch/type/source_url/**origin/status**)、source_hash 唯一键）+ 余弦相似度检索函数 + Repo searchKb + FileRepo mock（关键词匹配） | 迁移可重复执行；单测：检索命中/空结果/按 meta 过滤（含按 patch、**按 status=active** 过滤）正确；kb 表服务端专用（无 RLS，不经 anon 暴露） | T2 |
| T15 | 嵌入服务与入库管线（FR-11）：SiliconFlow bge-m3 适配器（env：EMBEDDING_API_KEY/BASE_URL/MODEL，OpenAI 兼容协议，1024 维；无密钥降级 mock 假向量）+ scripts/ingest-kb.mjs（读源文件 frontmatter → 切块 → 嵌入 → upsert，source_hash 幂等）+ **两个源目录**：kb/sources/（curated→active）与 kb/inferred/（inferred→candidate），初始骨架各含样例 | 入库脚本可运行且幂等（重复执行不重复插入）；两目录互不覆盖；样例入库后可检索命中（候选样例仅 status=candidate）；mock 模式全链路可测 | T14 |
| T16 | 分析时检索注入（FR-11 核心链路）：检索服务（query = 玩家 class/spec + 副本 + 章节/问答上下文 → 嵌入 → top-k≤5）→ 注入第 5 章与问答提示词（"【社区攻略参考】仅供参考，不代表本场数据"定界包裹 + 来源标注）；降级：未命中/无密钥/检索失败 → 仅 log 证据不报错 | 单测：注入格式正确、≤5 条、来源标注、降级不报错；**检索仅注入 status=active（候选条目绝不注入）**；检索按活跃补丁过滤（env ACTIVE_PATCH，缺省取最新入库 patch；patch=general 始终可见），旧补丁内容不注入；意图评测（intent-eval.test.ts）仍通过 | T14, T15 |
| T17 | FR-11 评测样例扩展：intent-samples.json 增加 ≥5 个"领域知识依赖型意图"案例（赌 buff 聚怪、留爆发对齐下一波易伤、按职业资源循环停手等）+ 配套知识条目 fixture；评测支持有检索/无检索双模式 | 知识依赖型案例在注入知识时正确率 ≥80%；评测脚本输出两种模式对比 | T16 |
| T18 | 初始知识库内容（第一批）：从 NGA 精华帖/Wowhead 整理 3 个当前赛季（至暗之夜 12.1，大秘境 S2）主流专精，每专精 ≥10 条（意图模式/爆发规划/资源管理要点），每条带 frontmatter（patch=12.1 + source_url 出处链接） | 条目格式合规、出处可打开验证；内容为要点摘要（非整篇搬运）；经主 Agent 审核入库 | T15 |
| T19 | 疑似高阶技巧判定（FR-5 第三档，用户要求）：第 5 章提示词增加第三档结论"疑似高阶技巧"——知识库解释不了但证据链完整的异常操作（或对比顶尖玩家存在同类操作），输出"疑似技巧 + 证据 + 推断理由"，不武断判失误；发现同步落库为候选条目（origin=inferred、status=candidate，不注入正式分析）；mock 提供器同步实现该档位 | 单测：构造样例（如转阶段前宠物提前就位规避落地伤害）判为"疑似"而非"失误"；候选落库可查且不注入用户报告；意图评测（intent-eval.test.ts）仍通过 | T16 |
| T20 | 高阶技巧批量挖掘工具（主动挖掘，用户新要求）：scripts/mine-patterns.mjs（团队侧离线脚本，非用户功能）——输入 = 同一高端玩家**同副本/日期相近**的多份 log（本地 WoWCombatLog.txt，或 WCL 链接在配额允许时）→ 逐份复用 FR-10 解析 + 意图引擎提取"可疑/疑似"操作模式 → 以副本时间轴（转阶段/易伤窗口）为锚**归一化相对时间** → **重复性检测**（同类型操作在 ≥2 份 log 的相似相对时间±容差/相似条件下重复出现）→ 输出候选条目 + 证据汇总（如"3/5 份 log 中，转阶段前 8±5 秒出现宠物提前移动"）+ 置信度评分 → 写入 kb/inferred/（origin=inferred、status=candidate） | 构造 ≥2 份含重复"宠物提前就位"模式的合成 log + 1 份对照 log → 工具输出该模式为高置信候选（附证据汇总），对照模式不误报；产出条目格式满足 ingest 要求；候选不注入正式分析 | T15, T19 |

## 与 PRD 需求映射（开发完成后 QA 对照）

| 任务 | 覆盖 PRD |
| --- | --- |
| T3 | FR-7（登录部分） |
| T4 | FR-2、FR-10 |
| T5 | FR-4 |
| T6 | FR-5 |
| T7 | FR-6 |
| T8 | FR-1、FR-3 |
| T9 | FR-7（额度部分） |
| T10 | FR-8 |
| T11 | FR-9 |
| T12/T13 | 非功能需求 + 全部 FR 的界面载体 |
| T14–T18 | FR-11（社区知识库 RAG） |
| T19 | FR-5 第三档（疑似高阶技巧）+ FR-11 候选沉淀 |
| T20 | FR-11 多 log 交叉挖掘（主动发现高阶技巧） |

## 知识保鲜运维（非开发任务，阶段 7 运营执行）

- 补丁触发更新（SLA ≤1 周）：调研员起草 → 主 Agent 审核 → `scripts/ingest-kb.mjs` 入库
- 社区反哺通道：玩家反馈新手法/纠错 → 验证 → 入库（运营阶段搭建反馈渠道）
- 活跃补丁切换：更新部署变量 ACTIVE_PATCH + 入库新内容（旧内容自动不再注入，保留可回滚）
- **候选技巧人工审查**：主 Agent 初审 + 内测期专家玩家（用户及其朋友）终审 → 转正（candidate→active）或弃用（deprecated）
- **多 log 交叉挖掘（批次 6 交付后启用）**：手动运行 `scripts/mine-patterns.mjs`，喂入顶尖玩家同副本/日期相近的多份 log → 重复模式产出候选 + 证据汇总 → 主 Agent 初审 + 内测专家终审转正
- 远期：顶尖玩家 log 全自动大规模聚类挖掘（第二版）

## 阶段 5 才做的部署事项（本清单不包含）

自定义域名与国内访问优化、WCL/DeepSeek/Resend 密钥申请与配置、监控与成本面板、GitHub 发布规范（README/LICENSE/CHANGELOG/tag/Release）。
