# 审计报告：WoW M+ AI 复盘教练

> 阶段 4（开发与质量）产出 · 汇总：主 Agent 独立验证 + QA 验收（docs/QA-REPORT.md）+ 安全审计（docs/SECURITY-REPORT.md）+ 修复与回归
> 结论版本：v1.0（2026-08-19）

## 结论（先看这里）

**✅ 达到可发布状态。** 功能验收（FR-1~FR-10）全部通过；87/87 单元测试通过；意图识别评测 16/16（100%）；类型检查 0 错误；生产构建成功；生产 fail-fast 安全机制实测生效；安全审计零致命/高危，3 个中危已全部修复并回归。剩余事项均为"阶段 5 部署阶段配置真实密钥后验证"，非代码缺陷。

## 一、功能验收（对照 PRD）

| 需求 | 结果 | 说明 |
| --- | --- | --- |
| FR-1 WCL 链接上传 | ✅ | www/cn 链接均通；团本/无效链接明确报错（QA HTTP 实测） |
| FR-2 原生文件上传 | ✅ | 浏览器本地解析（原始文件不上传）；合成样例验证战斗列表/报错（单测 + QA） |
| FR-3 选择战斗与对比对象 | ✅ | 战斗选择/对比可选/对比失败降级均验证 |
| FR-4 AI 复盘报告 | ✅ | 6 章并行 SSE 生成、120s 预算内（mock 实测）、可改进点带时间戳证据、中文+原名 |
| FR-5 战术意图理解 | ✅ | 样例集 16/16 全对（≥80% 门槛）；5:36 提前喝药案例正确归入"意图"章节 |
| FR-6 对话问答 | ✅ | 证据引用/10 轮上限/违规拒绝/通用建议标注全部实测通过 |
| FR-7 账号与登录 | ✅ | 验证码登录/错 5 锁 10 分钟/频控/每日 3 次额度（第 4 次 429）实测通过 |
| FR-8 历史记录 | ✅ | 列表/详情/删除级联；A/B 数据隔离 404 实测通过 |
| FR-9 一键分享 | ✅ | 128-bit token/免登录只读/关闭失效/删除级联失效；无效 token 已改真 404 |
| FR-10 log 预处理与降噪 | ✅ | ≤50K token 硬预算（服务端复算不可绕过）、缩减 ≥90%、时间戳一致、噪声不引用 |

> 标注：以上为 mock 模式验收结果。"部署阶段验证"清单见第四节（真实 Supabase RLS/RPC、Resend、DeepSeek、WCL、Turnstile、真实 log 回归）。

## 二、测试结果

| 项 | 结果 |
| --- | --- |
| 单元测试 | **87/87 通过**（13 文件，vitest，退出码 0） |
| FR-5 意图识别评测 | **16/16 = 100%**（规则引擎基线；真实模型评测在阶段 5 跑） |
| 类型检查 | `tsc --noEmit` 0 错误 |
| 生产构建 | `next build` 成功（全部路由生成） |
| 生产 fail-fast 冒烟 | 缺密钥时 /api/health 返回 **503 + 11 项缺失清单**；受保护接口硬失败（不静默降级）✅ |
| 页面 | /、/login、/history、/reports/[id]、3×/legal、/s/[token]（有效/无效）、404 页全部正常；"非暴雪官方"声明 100% 覆盖 |

## 三、安全检查（审计员独立执行，结论：零致命/高危）

- [x] 无硬编码密钥；`.env*` 不入库；git 历史无密钥；NEXT_PUBLIC 无服务端密钥
- [x] 无个人路径于代码（文档残留已清理）
- [x] 依赖安全：`npm audit` 0 漏洞；无 AGPL/GPL 传染（解析器自研；仅 1 个 LGPL 传递二进制备案）
- [x] 无 SQL 注入（全参数化）；无 SSRF（WCL 主机硬编码 + 仅 https）；无 XSS
- [x] 访问控制：全部受保护路由登录校验；A/B 数据隔离双层校验（服务层+repo 层）+ userId 守卫
- [x] 分享 token 128-bit 随机、可撤销、只读、无账户信息
- [x] 安全头覆盖页面与 API；CSP/防点击劫持/nosniff
- [x] 生产 fail-fast：密钥缺失拒绝服务（M-2 已修）
- [x] 额度原子计数（M-3 已修，`daily_usage` 唯一键 + RPC 原子 upsert）
- [x] 请求体大小上限（2MB）+ schema `.max()`；50K token 预算服务端复算
- [x] 错误响应统一友好文案，上游细节只进服务端日志

## 四、遗留问题（全部移交阶段 5，非代码缺陷）

| 问题 | 严重度 | 处理 |
| --- | --- | --- |
| 真实 Supabase RLS/RPC（迁移 0001/0002）需线上验证 | 部署验证项 | 阶段 5 配置真实 Supabase 后执行迁移并验证隔离 |
| 真实 DeepSeek：意图评测 ≥80%、报告 120s/问答 30s 压测、Vercel 60s 余量 | 部署验证项 | 阶段 5 配密钥后跑 `npm run eval:intent` + 压测 |
| 真实 WCL/Resend/Turnstile 行为 | 部署验证项 | 阶段 5 配置后按 FR-1/FR-7 验收标准复测 |
| ≥3 个真实 log 复跑 FR-2/FR-10 | 部署验证项 | 阶段 5 收集真实 log（含噪声、大文件）回归 |
| 频控为进程内存级（多实例失效） | 已接受风险 | 每日额度原子计数为最终防线；多实例部署前迁移 Redis（远期） |
| 额度在 enforce 阶段递增（校验失败也消耗次数） | 已接受风险 | 只烧用户自己的额度，体验可接受 |
| CSP 含 'unsafe-inline'（Next 内联脚本所需） | 已接受风险 | 备案，远期评估 nonce |
| README 仍为模板 | 阶段 5 必做 | 发布准备时替换（项目说明/环境变量/隐私/免责/非暴雪声明） |

## 五、修复记录（QA + 安全审计缺陷 → 工程师修复 → 回归）

| 提交 | 内容 |
| --- | --- |
| f00efc1 fix(security) | M-1 RLS 表述更正+userId 守卫；M-2 生产 fail-fast；L-1 安全头覆盖 API；L-4 错误文案；L-5 客户端 IP 解析；L-6 请求体上限；I-5 WCL 仅 https |
| 25a9c40 fix(quota) | M-3 每日额度原子计数（迁移 0002 + RPC） |
| 9acb0fb fix(ux) | D1 分享无效 token 真 404 |
| 65e981d perf(ai) | I-6 章节/问答按需数据切片（降成本） |
| 08ef770 chore | D2 vitest 配置；D3 注释更正；I-1 路径清理；I-2 .gitignore |
| 63d6e6b docs | QA 与安全报告归档 |

修复后回归：87/87 测试全绿（含新增 env 4 + 并发 1 用例）；意图评测 16/16；tsc 0 错误；构建成功；fail-fast 实测。

## 五-b、范围扩展批次记录（用户拍板新增，均为独立 QA + 安全审计 + 修复回归）

### 批次 5：RAG 社区知识库（FR-11，T14–T19）
- 交付：pgvector 迁移（kb_documents + 余弦检索 + 服务端专用权限）、SiliconFlow bge-m3 嵌入适配、ingest 双目录幂等入库（curated→active / inferred→candidate 互不覆盖）、检索注入（top-k≤5、status=active+ACTIVE_PATCH 过滤、随机定界防注入、来源标注、降级）、FR-5 第三档"疑似高阶技巧"（不武断判失误 + 候选沉淀）、3 专精初始知识内容（火法/兽王猎/防战）。
- QA：132/132 → 4 缺陷（D1 出处张冠李戴等，不阻塞）。安全审计：2 中危（M-RAG-1 定界符越狱注入、M-RAG-2 函数 PUBLIC 权限）。
- 修复：8287bb4（随机定界符+入库消毒+函数 revoke public/grant service_role+internal:inference 约定+hash 防碰撞）、50f1c85/c95382d/0e47224（D1–D4）。回归 144/144。

### 批次 6+7：高阶技巧挖掘 + 路线对比（FR-12，T20–T23）
- 交付：T20 多 log 交叉挖掘（证据汇总+置信度+幂等写入候选）、T21 战术波还原（chain 检测，死亡事件作濒死代理）、T22 路线指纹与相似度（0.5 内容+0.5 压平序列，合波不敏感；阵容画像含可替换职业）、T23 参考推荐排序与同路线分组（无数据优雅降级）。
- QA：165/165 → 5 缺陷（萨满定位 medium 等）。安全审计：零中危，3 低危。
- 修复：80ff9d7（萨满 hybrid、注释、flattenNames 封顶）、80fd1a0（写入时消毒、frontmatter 校验、CLI 输入上限）。回归 172/172。

### 当前全局基线
**172/172 测试全绿（24 文件）、tsc 0 错误、生产构建通过、npm audit 0 漏洞、零致命/高危/中危遗留。**

### 阶段 5 前置项（发布部署时必须处理，登记自各审计报告）
1. 真实 Supabase：执行迁移 0001–0003、启用 pgvector、核对函数 ACL 与 RLS、验证 A/B 隔离
2. 真实密钥接入：Supabase/Resend/DeepSeek/SiliconFlow(EMBEDDING_*)/WCL/Turnstile + 生产 fail-fast 生效
3. 真实模型 QA：eval:intent 双模式（知识依赖 ≥80%）、报告 120s/问答 30s 压测
4. ≥3 个真实 log 复跑 FR-2/FR-10/FR-12 验收；真实 WCL 字段名核对后接线"对比链接相似度展示"（from-link 目前仅元数据降级）
5. L-ROUTE-3（输入上限）已在 CLI 落实；接 Web 前复核复杂度约束
6. README 替换（含"非暴雪官方"声明）；自定义域名与国服访问；成本面板
7. 知识保鲜流程启动：ACTIVE_PATCH 配置、ingest 入库、候选人工审查通道（内测专家团）

## 六、Gate 4 判定

- [x] 任务清单全部完成（T1–T23 全绿，含用户新增的 RAG/路线/挖掘批次）
- [x] QA 对照 PRD（FR-1~FR-12）逐条验收，缺陷已全部处理或登记为阶段 5 前置项
- [x] 安全审计通过（四轮：主线 + RAG + 路线），全部中危修复并回归，零遗留中危
- [x] 审计报告留档，结论明确（本文件 + QA/SECURITY 系列报告）
- [ ] 用户确认质量达标（Gate 4b，等待中）
