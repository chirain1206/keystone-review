# 安全策略 / Security Policy

## 支持版本 / Supported Versions

仅当前最新版本（`main` 分支及最新 Release）获得安全更新。

| 版本 / Version | 支持 / Supported |
| --- | --- |
| 最新（main / 最新 Release） | ✅ 是 |
| 旧版本 | ❌ 否 |

## 报告漏洞 / Reporting a Vulnerability

如果你发现安全漏洞，**请不要在公开 issue 中披露**。请通过私下渠道联系维护者
（邮箱见仓库作者信息），说明：

1. 受影响的版本与入口（如某条 API 路由）。
2. 问题的复现步骤与影响范围。
3. 你建议的修复方向（如有）。

我们会在收到报告后尽快确认并回应，并在修复发布后与你确认是否公开致谢。

## 安全承诺 / Our Commitments

本产品处理用户的战斗日志与登录邮箱，我们对安全与隐私的底线承诺：

- 原始 `WoWCombatLog.txt` 只在浏览器本地解析，**绝不上传**原始文件。
- 绝不索取、收集、存储游戏账号密码。
- 接口层做登录鉴权与数据隔离，用户 A 无法访问用户 B 的数据。
- 密钥绝不进入仓库（`.env*` 已加入 `.gitignore`）。
- 检索知识与用户 log 均按"数据 / 指令隔离"处理，防提示词注入。
