name: probe-vercel

on:
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Probe deployment URL
        run: |
          echo "=== alias keystone-review.vercel.app ==="
          curl -s -D - -o /tmp/a.txt https://keystone-review.vercel.app/api/health || true
          head -20 /tmp/a.txt
          echo "=== deployment 3og37k766 ==="
          curl -s -D - -o /tmp/b.txt https://keystone-review-3og37k766-keystone-review.vercel.app/api/health || true
          head -20 /tmp/b.txt
